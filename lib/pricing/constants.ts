/**
 * Category id `1` is "Main" in this deployment: unit prices are segmented by `Customer.customerType`.
 * Other categories use a single direct price row per product (`customerType` null on schedule).
 */
export const MAIN_PRODUCT_CATEGORY_ID = 1;
